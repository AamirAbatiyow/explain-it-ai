
  # TikTok-style vertical feed UI

  This is a code bundle for TikTok-style vertical feed UI. The original project is available at https://www.figma.com/design/B2vR2vos4ObmkhQPC1egI6/TikTok-style-vertical-feed-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  